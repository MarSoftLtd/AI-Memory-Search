package com.bliss.aimemorysearch.ai.concepts;

/** Observational match grouping metadata for multi-concept image results. */
public enum MatchTier {
    BEST_MATCH,
    PARTIAL_MATCH,
    RELATED_MATCH
}
